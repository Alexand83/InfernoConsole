name: Build Windows Only

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build application
      run: npm run build

    - name: Build Electron app (Windows)
      run: npm run dist:win:nsis

    - name: Upload Windows artifacts
      uses: actions/upload-artifact@v4
      with:
        name: windows-build-${{ github.ref_name }}
        path: dist-electron/*.exe

  release-windows:
    needs: build-windows
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download Windows artifacts
      uses: actions/download-artifact@v4
      with:
        name: windows-build-${{ github.ref_name }}
        path: dist/windows

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ github.ref_name }}
        name: DJ Console Pro ${{ github.ref_name }} (Windows)
        body: |
          ## 🎵 DJ Console Pro ${{ github.ref_name }} - Windows
          
          ### ✨ Funzionalità
          - Sistema di streaming continuo migliorato
          - Fix auto-reconnessione dopo disconnessione manuale
          - Nickname DJ personalizzabile per Icecast
          - Auto-update funzionante
          
          ### 🐛 Bug Fix
          - Risolto errore "Cannot find module electron-updater"
          - Fix auto-reconnessione indesiderata
          - Migliorato controllo deck audio
          
          ### 📦 Download
          **Windows**: Installa il file `.exe` per Windows 10/11
          
          ### 🚀 Installazione
          1. Scarica il file `.exe` appropriato per la tua architettura
          2. Esegui l'installer
          3. Avvia DJ Console Pro e goditi la tua sessione DJ!
          
          ### 📝 Note
          - Versione sviluppata da Alessandro(NeverAgain)
          - Licenza MIT
          - Supporto completo per streaming Icecast
          - Auto-update abilitato
        files: |
          dist/windows/*
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
