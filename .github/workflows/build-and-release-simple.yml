name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build Electron app (Windows)
      if: matrix.os == 'windows-latest'
      run: npm run dist:win:portable
      env:
        GH_TOKEN: ${{ secrets.GH_TOKEN }}

    - name: Verify Portable Build (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        echo "Checking for portable executable..."
        if (Test-Path "dist-electron\Inferno-Console-win.exe") {
          echo "✅ Portable executable found"
          Get-Item "dist-electron\Inferno-Console-win.exe" | Select-Object Name, Length
        } else {
          echo "❌ Portable executable not found"
          Get-ChildItem "dist-electron" | Select-Object Name, Length
        }

    - name: Build Custom Installer (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        cd installer
        npm install
        npm install -g pkg
        node build-installer.js
      env:
        GH_TOKEN: ${{ secrets.GH_TOKEN }}

    - name: Verify All Files Created (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        echo "Checking all release files..."
        $requiredFiles = @(
          "dist-electron\Inferno-Console-Installer.exe",
          "dist-electron\Inferno-Console-Uninstaller.exe", 
          "dist-electron\Inferno-Console-win.exe",
          "dist-electron\latest.yml"
        )
        
        foreach ($file in $requiredFiles) {
          if (Test-Path $file) {
            $size = (Get-Item $file).Length
            echo "✅ $file - $([math]::Round($size/1MB, 2)) MB"
          } else {
            echo "❌ $file - MISSING"
          }
        }

    - name: Create Release (Windows)
      if: matrix.os == 'windows-latest' && startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ github.ref_name }}
        name: Inferno Console ${{ github.ref_name }}
        body: |
          ## 🎵 Inferno Console ${{ github.ref_name }}
          
          ### ✨ Nuove Funzionalità
          - Sistema di streaming continuo migliorato
          - Fix auto-reconnessione dopo disconnessione manuale
          - Nickname DJ personalizzabile per Icecast
          - Interfaccia utente ottimizzata
          - **Installer GUI personalizzato** con tema scuro
          - **Selezione directory** personalizzabile
          - **Integrazione electron-updater** per percorsi custom
          - **Risoluzione dinamica URL** per download da GitHub Releases
          - **Fix errori navigazione** e loop infiniti
          
          ### 🐛 Bug Fix
          - Risolto problema di auto-reconnessione indesiderata
          - Migliorato controllo deck audio durante disconnessione
          - Fix logging per debug del sistema di retry
          - Fix installer pkg non trovato in GitHub Actions
          
          ### 📦 Download
          **Windows**: Scarica `Inferno-Console-Installer.exe` - Installer GUI personalizzato
          
          ### 🚀 Installazione
          1. Scarica `Inferno-Console-Installer.exe`
          2. Esegui l'installer (interfaccia moderna)
          3. Scegli la directory di installazione
          4. L'app si avvia automaticamente!
          
          ### ✨ Caratteristiche Installer
          - **Singolo file .exe** - tutto incluso (~38MB)
          - **Interfaccia moderna** - tema scuro NSIS-style
          - **Selezione directory** - installa dove vuoi
          - **Shortcut automatici** - Desktop e Start Menu
          - **Uninstaller integrato** - rimozione completa
          - **Compatibile con electron-updater** - aggiornamenti automatici
          - **Backup automatico** - sicurezza durante aggiornamenti
          
          ### 📝 Note
          - Versione sviluppata da Alessandro(NeverAgain)
          - Licenza MIT
          - Supporto completo per streaming Icecast
          - Aggiornamenti automatici rispettano directory personalizzata
        files: |
          dist-electron/Inferno-Console-Installer.exe
          dist-electron/Inferno-Console-Uninstaller.exe
          dist-electron/Inferno-Console-win.exe
          dist-electron/latest.yml
        draft: false
        prerelease: false
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
      continue-on-error: true